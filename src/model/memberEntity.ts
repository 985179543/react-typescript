// entity 实体
// typescript 独有的
// 数据 类型的严谨 格式
// 接口 member实现的属性
// 要数据，先定义接口 接口以;结束
// 大型数据 model 放数据接口定义
// 都必须实现 模型要求
export interface MemberEntity {
    id: number; //1213123
    login: string;
    avatar_url: string;
}